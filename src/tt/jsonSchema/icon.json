{
  "title": "icon",
  "type": "object",
  "properties": {
    "type": {
      "type": "string",
      "description": "icon 的类型",
      "enum": [
        "success",
        "success_no_circle",
        "info",
        "warn",
        "waiting",
        "clear",
        "cancel",
        "download",
        "search"
      ]
    },
    "size": {
      "type": "number",
      "description": "icon 的大小，单位 px"
    },
    "color": {
      "type": "string",
      "description": "icon 的颜色"
    }
  },
  "additionalProperties": false,
  "required": [
    "type"
  ]
}