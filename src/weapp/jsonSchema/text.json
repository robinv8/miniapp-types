{
  "title": "text",
  "type": "object",
  "properties": {
    "selectable": {
      "type": "boolean",
      "description": "文本是否可选 (已废弃)"
    },
    "user-select": {
      "type": "boolean",
      "description": "文本是否可选，该属性会使文本节点显示为 inline-block"
    },
    "space": {
      "type": "string",
      "description": "显示连续空格",
      "enum": [
        "ensp",
        "emsp",
        "nbsp"
      ]
    },
    "decode": {
      "type": "boolean",
      "description": "是否解码"
    }
  },
  "additionalProperties": false
}