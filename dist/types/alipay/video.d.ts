/* tslint:disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

export interface Video {
  /**
   * 内联样式。
   */
  style?: string;
  /**
   * 外部样式名。
   */
  class?: string;
  /**
   * 要播放视频的资源地址，支持网络地址，支持优酷视频编码（支付宝客户端 10.1.75）。
   * src 支持的协议如下：
   * vid/showId: XMzg2Mzc5MzMwMA==
   * apFilePath: https://resource/xxx.video。
   */
  src?: string;
  /**
   * 视频封面图的 url，支持 jpg、png 等图片，如 https://***.jpg。如果不传的话，默认取视频的首帧图作为封面图。
   */
  poster?: string;
  /**
   * 当 poster 高宽比跟视频高宽不匹配时，如何显示 poster，设置规则同 background-size 一致。
   */
  "poster-size"?: string;
  /**
   * 当视频大小与 video 容器大小不一致时，视频的表现形式。contain：包含，fill：填充。
   */
  "object-fit"?: string;
  /**
   * 指定视频初始播放位置，单位 s。
   */
  "initial-time"?: number;
  /**
   * 为无法读取时长的视频设置时长，单位 s。
   */
  duration?: number;
  /**
   * 是否显示默认播放控件（底部工具条，包括播放/暂停按钮、播放进度、时间）。
   */
  controls?: boolean;
  /**
   * 是否自动播放。
   */
  autoplay?: boolean;
  /**
   * 设置全屏时视频的方向，不指定则根据宽高比自动判断。有效值为 0（正常竖向）, 90（屏幕逆时针 90 度）, -90（屏幕顺时针 90 度）
   */
  direction?: number;
  /**
   * 是否循环播放。
   */
  loop?: boolean;
  /**
   * 是否静音播放。
   */
  muted?: boolean;
  /**
   * 是否显示全屏按钮。
   */
  "show-fullscreen-btn"?: boolean;
  /**
   * 是否显示视频底部控制栏的播放按钮。
   */
  "show-play-btn"?: boolean;
  /**
   * 是否显示视频中间的播放按钮。
   */
  "show-center-play-btn"?: boolean;
  /**
   * 是否展示工具栏上的静音按钮。
   */
  "show-mute-btn"?: boolean;
  /**
   * 当底部工具条隐藏时，是否显示细进度条（controls=false 时设置无效）。
   */
  "show-thin-progress-bar"?: boolean;
  /**
   * 全屏模式下是否开启控制进度的手势。
   */
  "enable-progress-gesture"?: boolean;
  /**
   * 移动网络提醒样式。
   *
   * 0 - 不提醒
   * 1 - tip 提醒
   * 2 - 阻塞提醒(无消耗流量大小)
   * 3 - 阻塞提醒(有消耗流量大小提醒)
   */
  "mobilenet-hint-type"?: number;
  /**
   * 当开始/继续播放时触发 play 事件。
   */
  onPlay?: () => void;
  /**
   * 当暂停播放时触发 pause 事件。
   */
  onPause?: () => void;
  /**
   * 当播放到末尾时触发 ended 事件。
   */
  onEnded?: () => void;
  /**
   * 播放进度变化时触发，event.detail = {currentTime: '当前播放时间',userPlayDuration:'用户实际观看时长',videoDuration:'视频总时长'}。
   */
  onTimeUpdate?: () => void;
  /**
   * 视频出现缓冲时触发。
   */
  onLoading?: () => void;
  /**
   * 视频播放出错时触发（errorCode 见下面错误码表）。
   */
  onError?: () => void;
  /**
   * 视频进入和退出全屏时触发，event.detail = {fullScreen, direction}，direction 取为 vertical 或 horizontal。
   */
  onFullScreenChange?: () => void;
  /**
   * 点击视频 view 时触发，event.detail = {ptInView:{x:0,y:0}}。
   */
  onTap?: () => void;
  /**
   * 用户操作事件，event.detail = {tag:"mute", value:0}，tag 为用户操作的元素，目前支持的 tag 有：play（底部播放按钮）、centerplay（中心播放按钮）、mute（静音按钮）、fullscreen（全屏按钮）、retry（重试按钮）、mobilenetplay（网络提醒的播放按钮）。
   */
  onUserAction?: () => void;
  /**
   * 视频播放终止。
   */
  onStop?: () => void;
  /**
   * 当视频加载完真正开始播放时触发。
   */
  onRenderStart?: () => void;
  /**
   * 浮窗设置。暂时不支持全局浮窗。
   * 可选值：
   *
   * none：无浮窗。
   * page：页面内浮窗。
   */
  "floating-mode"?: string;
}
