{
  "title": "radio",
  "type": "object",
  "properties": {
    "value": {
      "type": "string",
      "description": "标识。当该 选中时， 的 change 事件会携带 的 value。"
    },
    "checked": {
      "type": "boolean",
      "description": "当前是否选中。",
      "defaultValue": "false"
    },
    "disabled": {
      "type": "boolean",
      "description": "是否禁用。",
      "defaultValue": "false"
    },
    "color": {
      "type": "string",
      "description": "radio 的颜色，同 CSS 的 color 。",
      "defaultValue": "#F85959"
    }
  },
  "additionalProperties": false
}